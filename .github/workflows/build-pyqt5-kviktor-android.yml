name: build-pyqt5-kviktor-android

# PyQt5 5.15.1 / Python 3.7.7 / Qt 5.13.2 -> Android APK builder.
#
# Wraps the pyqt5_android_kviktor.py orchestrator from
# https://github.com/kviktor/pyqtdeploy-android-build inside an
# Ubuntu 20.04 Docker container that has the EXACT preinstalled
# toolchain the kviktor guide specifies:
#
#     Qt 5.13.2 Android arm64-v8a   (via aqtinstall)
#     Android NDK r20b
#     Android SDK API 28 + build-tools 28.0.3
#     OpenJDK 8
#     Python 3.7  (from deadsnakes PPA)
#     pyqtdeploy 2.5.1
#
# The Docker image is heavy (~5-6 GB once built) but cached aggressively
# via GHA's Docker layer cache.  First run: ~25-30 min.  Subsequent
# runs: ~5-8 min (image cached, source tarballs cached, only the actual
# build runs).
#
# Total APK output: ~30-40 MB for a hello-world PyQt5 app.
#
# Inputs:
#   project_url     Public URL to a zip containing main.py at root
#   app_name        Application display name
#   package_name    Android package id (com.example.X format)
#   install_apk     Set true to run `adb install` (needs adb USB device,
#                   so really only useful for self-hosted runners)

on:
  workflow_dispatch:
    inputs:
      project_url:
        description: "Public URL to PyQt5 project zip (must contain main.py at root)."
        required: true
        default: "https://raw.githubusercontent.com/Usama01TN/PyQtBuilder/main/testapp_qt5.zip"
      app_name:
        description: "App display name."
        required: false
        default: "TestApp"
      package_name:
        description: "Android package id."
        required: false
        default: "com.example.testapp"
      jobs:
        description: "Parallel make jobs (kviktor default: 2)."
        required: false
        default: "2"

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90

    env:
      PROJECT_URL:  ${{ github.event.inputs.project_url }}
      APP_NAME:     ${{ github.event.inputs.app_name }}
      PACKAGE_NAME: ${{ github.event.inputs.package_name }}
      JOBS:         ${{ github.event.inputs.jobs }}

    steps:
      - name: Checkout repo (for builder script + Dockerfile)
        uses: actions/checkout@v4

      - name: Verify required files are in repo
        run: |
          MISSING=0
          for f in pyqt5_android_kviktor.py build_utils.py builders.py Dockerfile.pyqt5-kviktor; do
            if [ ! -f "$f" ]; then
              echo "::error::$f missing from repo root."
              MISSING=$((MISSING+1))
            fi
          done
          if [ "$MISSING" -gt 0 ]; then
            echo "::error::Place the listed files at the repo root (next to your workflow YAML) and commit."
            exit 1
          fi
          echo "Builder file checksums (for log traceability):"
          sha256sum pyqt5_android_kviktor.py build_utils.py builders.py Dockerfile.pyqt5-kviktor

      - name: Free disk space (Docker image + Qt + NDK need ~15 GB)
        run: |
          echo "BEFORE:"; df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost \
                      /usr/local/lib/android/sdk \
                      /opt/hostedtoolcache/CodeQL \
                      "$AGENT_TOOLSDIRECTORY"
          sudo apt-get -y purge --auto-remove \
              azure-cli google-cloud-cli microsoft-edge-stable \
              google-chrome-stable firefox powershell 2>/dev/null || true
          docker system prune -af 2>/dev/null || true
          echo "AFTER:"; df -h /

      - name: Set up Docker Buildx (for GHA layer cache)
        uses: docker/setup-buildx-action@v3

      - name: Compute Dockerfile hash (cache invalidation key)
        id: dockerhash
        run: |
          HASH=$(sha256sum Dockerfile.pyqt5-kviktor | awk '{print substr($1, 1, 12)}')
          echo "hash=$HASH" >> "$GITHUB_OUTPUT"
          echo "================================================================"
          echo "  Dockerfile.pyqt5-kviktor SHA256 (first 12): $HASH"
          echo "  GHA cache key: pyqt5-kviktor-img-$HASH-v2"
          echo "  This prevents using a cached image from a previous"
          echo "  (broken) version of the Dockerfile."
          echo "================================================================"
          # Print the relevant lines for sanity
          echo "  apt install block (first 20 lines):"
          grep -nB0 -A20 "^RUN set -ex" Dockerfile.pyqt5-kviktor | head -25 || true
          echo "================================================================"

      - name: Build pyqt5-kviktor-builder Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.pyqt5-kviktor
          tags: pyqt5-kviktor-builder:latest
          load: true
          # The cache key includes the Dockerfile hash so any change
          # to the file forces a fresh build.  This prevents BuildKit
          # from reusing a layer cached from a previous broken version.
          cache-from: type=gha,scope=pyqt5-kviktor-${{ steps.dockerhash.outputs.hash }}-v2
          cache-to:   type=gha,mode=max,scope=pyqt5-kviktor-${{ steps.dockerhash.outputs.hash }}-v2

      - name: Show Docker image contents
        run: docker run --rm pyqt5-kviktor-builder

      - name: Extract Qt 5.13.2 Android arm64 as a downloadable tarball
        # The image bakes Qt 5.13.2 Android arm64-v8a at /root/Qt/5.13.2.
        # We export it here as a standalone tarball so it can be reused:
        #
        #   * As a GHA artifact (downloadable for 90 days)
        #   * As an attachment to a GitHub Release (permanent mirror)
        #   * As a manual `--qt-dir` input for future builds that don't
        #     want to rebuild this entire image (skip the aqtinstall step)
        #
        # Tarball is about 1 GB compressed.  We use pigz for parallel
        # compression if available, otherwise plain gzip.
        run: |
          mkdir -p artifacts
          docker run --rm -v "$(pwd)/artifacts:/out" pyqt5-kviktor-builder \
              bash -c '
                cd /root/Qt && \
                if command -v pigz >/dev/null; then \
                  COMPRESS="--use-compress-program=pigz"; \
                else \
                  COMPRESS="--gzip"; \
                fi && \
                tar -cf /out/qt-5.13.2-android-arm64-v8a.tar.gz \
                    $COMPRESS 5.13.2/android_arm64_v8a 5.13.2/Src 2>/dev/null \
                    || tar -czf /out/qt-5.13.2-android-arm64-v8a.tar.gz 5.13.2/android_arm64_v8a
                # Also dump checksum + manifest of what is inside
                ls -lh /out/qt-5.13.2-android-arm64-v8a.tar.gz
                sha256sum /out/qt-5.13.2-android-arm64-v8a.tar.gz | tee /out/qt-5.13.2-android-arm64-v8a.sha256
                tar tzf /out/qt-5.13.2-android-arm64-v8a.tar.gz | head -20 > /out/qt-5.13.2-android-arm64-v8a.manifest.txt
                tar tzf /out/qt-5.13.2-android-arm64-v8a.tar.gz | wc -l >> /out/qt-5.13.2-android-arm64-v8a.manifest.txt
              '
          ls -lh artifacts/

      - name: Upload Qt 5.13.2 Android arm64 as artifact
        uses: actions/upload-artifact@v4
        with:
          name: qt-5.13.2-android-arm64-v8a
          path: |
            artifacts/qt-5.13.2-android-arm64-v8a.tar.gz
            artifacts/qt-5.13.2-android-arm64-v8a.sha256
            artifacts/qt-5.13.2-android-arm64-v8a.manifest.txt
          retention-days: 90
          # Don't fail if compression of a partial layout happens (the tar
          # `|| true` above handles missing optional dirs).
          if-no-files-found: error

      - name: Extract Android NDK r20b as a downloadable tarball
        run: |
          docker run --rm -v "$(pwd)/artifacts:/out" pyqt5-kviktor-builder \
              tar -czf /out/android-ndk-r20b.tar.gz \
                  -C /root/Android android-ndk-r20b
          ls -lh artifacts/android-ndk-r20b.tar.gz
          docker run --rm -v "$(pwd)/artifacts:/out" pyqt5-kviktor-builder \
              bash -c 'sha256sum /out/android-ndk-r20b.tar.gz' \
              | tee artifacts/android-ndk-r20b.sha256

      - name: Upload Android NDK r20b as artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-ndk-r20b
          path: |
            artifacts/android-ndk-r20b.tar.gz
            artifacts/android-ndk-r20b.sha256
          retention-days: 90
          if-no-files-found: error

      - name: Extract Android SDK API 28 as a downloadable tarball
        # SDK is smaller than Qt/NDK (~400 MB) but worth saving for
        # offline reproducibility.
        run: |
          docker run --rm -v "$(pwd)/artifacts:/out" pyqt5-kviktor-builder \
              tar -czf /out/android-sdk-api28.tar.gz \
                  -C /root/Android tools
          ls -lh artifacts/android-sdk-api28.tar.gz
          docker run --rm -v "$(pwd)/artifacts:/out" pyqt5-kviktor-builder \
              bash -c 'sha256sum /out/android-sdk-api28.tar.gz' \
              | tee artifacts/android-sdk-api28.sha256

      - name: Upload Android SDK API 28 as artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-sdk-api28
          path: |
            artifacts/android-sdk-api28.tar.gz
            artifacts/android-sdk-api28.sha256
          retention-days: 90
          if-no-files-found: error

      - name: Cache build artifacts (source tarballs, sysroot, build dirs)
        uses: actions/cache@v4
        with:
          path: /home/runner/kviktor-cache
          key: pyqt5-kviktor-${{ hashFiles('pyqt5_android_kviktor.py') }}-v1
          restore-keys: |
            pyqt5-kviktor-

      - name: Download user project
        run: |
          mkdir -p workspace
          curl -L --fail --retry 3 "$PROJECT_URL" -o project.zip
          unzip -q -o project.zip -d workspace
          MAIN_PY=$(find workspace -maxdepth 4 -name main.py -type f | head -1)
          if [ -z "$MAIN_PY" ]; then
            echo "::error::No main.py found in project zip"
            exit 1
          fi
          PROJ_DIR=$(dirname "$MAIN_PY")
          echo "PROJ_DIR=$PROJ_DIR" >> "$GITHUB_ENV"

          # Print the main.py so we can verify it's actually a PyQt5 app
          echo "================================================================"
          echo "  main.py at $MAIN_PY"
          echo "  ($(stat -c %s "$MAIN_PY") bytes)"
          echo "================================================================"
          cat -n "$MAIN_PY"
          echo "================================================================"
          echo "  Import scan:"
          grep -nE "^\s*(from|import)\s+(PyQt5|PySide|PyQt6|PySide2|PySide6)\b" "$MAIN_PY" || \
              echo "  (no Qt-binding imports found — this app may not be a Qt app)"
          echo "================================================================"

      - name: Patch broken PyPI download URLs in kviktor script
        # The kviktor script hardcodes URLs like
        #     https://pypi.org/packages/source/s/sip/sip-4.19.24.tar.gz
        # which now return HTTP 404 because PyPI removed the
        # /packages/source/<letter>/<pkg>/ URL layout years ago (it now
        # uses files.pythonhosted.org with hash-prefixed paths), and
        # legacy tarballs like SIP 4.19.24 aren't republished there.
        #
        # MacPorts mirrors PyQt/SIP source tarballs at
        # distfiles.macports.org under /py-<pkgname>/.  We patch in
        # place before running the builder.
        #
        # We also pre-download the SIP tarball into the project's
        # sources/ directory so that if MacPorts is unreachable, we
        # already have the file and the kviktor script skips its own
        # (now patched) download attempt.  Multiple mirrors are tried
        # with fallback.
        run: |
          set -e

          # ── (1) sed-patch the URLs in the kviktor script ──
          echo "Before patch:"
          grep -nE "pypi\.org/packages|distfiles\.macports|sip-4.19.24|PyQt5-5.15.1" \
               pyqt5_android_kviktor.py | head -10 || true
          echo "================================================================"

          # SIP: pypi.org/packages/source/s/sip/sip-X  ->  distfiles.macports.org/py-sip/sip-X
          sed -i 's|https://pypi.org/packages/source/s/sip/sip-|https://distfiles.macports.org/py-sip/sip-|g' \
              pyqt5_android_kviktor.py

          # PyQt5: same pattern (if present in the script — sed is a no-op if not).
          # PyPI uses lowercase /p/ in old URLs but the tarball is PyQt5-X.tar.gz.
          sed -i 's|https://pypi.org/packages/source/p/PyQt5/PyQt5-|https://distfiles.macports.org/py-pyqt5/PyQt5-|g' \
              pyqt5_android_kviktor.py
          sed -i 's|https://pypi.org/packages/source/P/PyQt5/PyQt5-|https://distfiles.macports.org/py-pyqt5/PyQt5-|g' \
              pyqt5_android_kviktor.py

          echo "After patch:"
          grep -nE "distfiles\.macports|pypi\.org/packages|sip-4.19.24|PyQt5-5.15.1" \
               pyqt5_android_kviktor.py | head -10 || true
          echo "================================================================"

          # ── (2) Pre-download SIP into the project's sources/ dir ──
          # The kviktor script downloads sources to /work/<projname>/sources/.
          # If the file is already there, the script skips downloading it.
          # We try 3 mirrors with fallback.
          SOURCES_DIR="$PROJ_DIR/sources"
          mkdir -p "$SOURCES_DIR"
          SIP_FILE="$SOURCES_DIR/sip-4.19.24.tar.gz"
          MIN_SIP_BYTES=$((900 * 1024))   # SIP 4.19.24 is ~1 MB

          SIP_MIRRORS=(
              "https://distfiles.macports.org/py-sip/sip-4.19.24.tar.gz"
              "https://sourceforge.net/projects/pyqt/files/sip/sip-4.19.24/sip-4.19.24.tar.gz/download"
          )
          for url in "${SIP_MIRRORS[@]}"; do
              echo "  Trying SIP mirror: $url"
              if curl -L --fail --retry 2 --max-time 60 -A 'Mozilla/5.0' \
                      -o "$SIP_FILE" "$url"; then
                  size=$(stat -c %s "$SIP_FILE")
                  if [ "$size" -ge "$MIN_SIP_BYTES" ]; then
                      echo "  ✓ Got SIP from $url ($size bytes)"
                      break
                  else
                      echo "  ! Too small ($size bytes), trying next"
                      rm -f "$SIP_FILE"
                  fi
              fi
          done

          if [ ! -f "$SIP_FILE" ] || [ "$(stat -c %s "$SIP_FILE")" -lt "$MIN_SIP_BYTES" ]; then
              echo "::warning::Could not pre-download SIP — kviktor will try its patched URL"
          else
              echo "  Pre-downloaded $SIP_FILE"
              echo "  SHA256: $(sha256sum "$SIP_FILE" | awk '{print $1}')"
          fi

          # ── (3) Patch OpenSSL URL and pre-download ──
          # The kviktor script downloads OpenSSL from
          #     https://www.openssl.org/source/old/1.0.2/openssl-1.0.2r.tar.gz
          # which now returns HTTP 404.  OpenSSL 1.0.2 reached EOL on
          # 2019-12-31 and the OpenSSL Project pruned their /source/old/
          # archives.  GitHub Releases mirror the canonical tarballs.
          echo
          echo "Patching OpenSSL URL ..."

          # Replace any flavor of the dead URL.  We match on
          # "openssl.org/source/" so both /source/old/X.Y/ and the
          # newer /source/ paths get redirected to GitHub.
          sed -i 's|https://www.openssl.org/source/old/1.0.2/openssl-|https://github.com/openssl/openssl/releases/download/OpenSSL_1_0_2r/openssl-|g' \
              pyqt5_android_kviktor.py
          sed -i 's|https://www.openssl.org/source/openssl-1.0.2r|https://github.com/openssl/openssl/releases/download/OpenSSL_1_0_2r/openssl-1.0.2r|g' \
              pyqt5_android_kviktor.py

          echo "OpenSSL URL after patch:"
          grep -nE "openssl-1.0.2r|openssl\.org/source|openssl/releases" \
               pyqt5_android_kviktor.py | head -5 || true
          echo "================================================================"

          OPENSSL_FILE="$SOURCES_DIR/openssl-1.0.2r.tar.gz"
          MIN_OPENSSL_BYTES=$((4 * 1024 * 1024))  # OpenSSL 1.0.2r is ~5 MB

          OPENSSL_MIRRORS=(
              "https://github.com/openssl/openssl/releases/download/OpenSSL_1_0_2r/openssl-1.0.2r.tar.gz"
              "https://distfiles.macports.org/openssl10/openssl-1.0.2r.tar.gz"
              "https://artfiles.org/openssl.org/source/old/1.0.2/openssl-1.0.2r.tar.gz"
          )
          for url in "${OPENSSL_MIRRORS[@]}"; do
              echo "  Trying OpenSSL mirror: $url"
              if curl -L --fail --retry 2 --max-time 120 -A 'Mozilla/5.0' \
                      -o "$OPENSSL_FILE" "$url"; then
                  size=$(stat -c %s "$OPENSSL_FILE")
                  if [ "$size" -ge "$MIN_OPENSSL_BYTES" ]; then
                      echo "  ✓ Got OpenSSL from $url ($size bytes)"
                      break
                  else
                      echo "  ! Too small ($size bytes), trying next"
                      rm -f "$OPENSSL_FILE"
                  fi
              fi
          done

          if [ ! -f "$OPENSSL_FILE" ] || [ "$(stat -c %s "$OPENSSL_FILE")" -lt "$MIN_OPENSSL_BYTES" ]; then
              echo "::warning::Could not pre-download OpenSSL — kviktor will try its patched URL"
          else
              echo "  Pre-downloaded $OPENSSL_FILE"
              echo "  SHA256: $(sha256sum "$OPENSSL_FILE" | awk '{print $1}')"
          fi

          echo "================================================================"
          echo "Patched script SHA256: $(sha256sum pyqt5_android_kviktor.py | awk '{print $1}')"

      - name: Run kviktor builder inside container
        run: |
          set -o pipefail
          mkdir -p /home/runner/kviktor-cache /home/runner/kviktor-output

          # Mount:
          #   /work          -> the GHA workspace (where our scripts + project live)
          #   /cache         -> persistent cache (source tarballs, build dirs)
          #   /output        -> where the APK gets copied at the end
          docker run --rm \
            -v "${GITHUB_WORKSPACE}:/work" \
            -v "/home/runner/kviktor-cache:/cache" \
            -v "/home/runner/kviktor-output:/output" \
            -w /work \
            -e JOBS="$JOBS" \
            pyqt5-kviktor-builder \
            bash -c '
              set -e
              # The kviktor script defaults to ~/Qt and ~/Android which already
              # exist in our image (installed in the Dockerfile).  We override
              # nothing; the defaults match the image layout exactly.

              # The kviktor builder writes intermediate state to its own
              # workspace dir.  Symlink that under /cache so it survives
              # between runs.
              KVIKTOR_WORK="/root/.cache/pyqt5-kviktor-build"
              mkdir -p /cache/kviktor-work
              rm -rf "$KVIKTOR_WORK"
              ln -s /cache/kviktor-work "$KVIKTOR_WORK"

              # Activate the prebuilt venv (image has pyqtdeploy 2.5.1 in there)
              # so the builder finds pyqtdeploy-sysroot + pyqtdeploy-build.
              . /root/build-venv/bin/activate

              # ──────────────────────────────────────────────────────────
              # Make the kviktor venv reuse our pre-built one (via symlink)
              # ──────────────────────────────────────────────────────────
              # IMPORTANT: the kviktor script creates its OWN venv at
              # /root/.pyqt5_android_kviktor/venv -- it ignores any venv
              # we pre-built elsewhere.  Look at lines 185-187 + 468-472
              # of pyqt5_android_kviktor.py:
              #     base = join(_HOME, ".pyqt5_android_kviktor")
              #     self.venv_dir = join(base, "venv")
              #     ...
              #     if exists(cfg.venv_dir):
              #         log.info("Reusing venv: %s", cfg.venv_dir)
              #     else:
              #         create(cfg.venv_dir, with_pip=True, clear=True)
              #
              # If the path already exists, kviktor reuses it.  So we
              # symlink the kviktor venv path -> our pre-built /root/build-venv.
              # The setup_venv step in kviktor then sees "Reusing venv" and
              # skips re-creation.  Its pip install still runs (which may
              # downgrade PyQt5 from 5.15.7 -> 5.15.1 to match kviktor's
              # expected version), but that succeeds quickly and preserves
              # our case-aliased plugins.
              mkdir -p /root/.pyqt5_android_kviktor
              if [ ! -e /root/.pyqt5_android_kviktor/venv ]; then
                  ln -sfn /root/build-venv /root/.pyqt5_android_kviktor/venv
                  echo "Symlinked /root/.pyqt5_android_kviktor/venv -> /root/build-venv"
              fi
              ls -ld /root/.pyqt5_android_kviktor/venv

              # ──────────────────────────────────────────────────────────
              # Create TitleCase aliases for pyqtdeploy plugins.
              # ──────────────────────────────────────────────────────────
              # The kviktor sysroot.json uses TitleCase keys:
              #     "OpenSSL", "Python", "PyQt5", "SIP", "Qt"
              # but pyqtdeploy 2.5.1 ships its plugins with lowercase filenames:
              #     openssl.py, python/, pyqt5.py, sip.py, qt5.py
              # pyqtdeploy does `importlib.import_module(".plugins." + name)`
              # which is case-sensitive on Linux, so it fails with:
              #     pyqtdeploy-sysroot: unable to find a plugin for "OpenSSL"
              # Fix: cp the plugins under TitleCase names so both imports work.
              # Note: "Qt" maps to qt5.py (not qt.py -- pyqtdeploy 2.5.1
              # only has the qt5 plugin).
              #
              # Because of the symlink above, the aliases apply to BOTH paths.
              PLUGINS_DIR=/root/build-venv/lib/python3.7/site-packages/pyqtdeploy/sysroot/plugins
              echo "Creating TitleCase plugin aliases in $PLUGINS_DIR"
              [ -f "$PLUGINS_DIR/openssl.py" ] && [ ! -f "$PLUGINS_DIR/OpenSSL.py" ] && cp "$PLUGINS_DIR/openssl.py" "$PLUGINS_DIR/OpenSSL.py"
              [ -f "$PLUGINS_DIR/sip.py"     ] && [ ! -f "$PLUGINS_DIR/SIP.py"     ] && cp "$PLUGINS_DIR/sip.py"     "$PLUGINS_DIR/SIP.py"
              [ -f "$PLUGINS_DIR/pyqt5.py"   ] && [ ! -f "$PLUGINS_DIR/PyQt5.py"   ] && cp "$PLUGINS_DIR/pyqt5.py"   "$PLUGINS_DIR/PyQt5.py"
              [ -f "$PLUGINS_DIR/qt5.py"     ] && [ ! -f "$PLUGINS_DIR/Qt.py"      ] && cp "$PLUGINS_DIR/qt5.py"     "$PLUGINS_DIR/Qt.py"
              [ -d "$PLUGINS_DIR/python"     ] && [ ! -d "$PLUGINS_DIR/Python"     ] && cp -r "$PLUGINS_DIR/python"  "$PLUGINS_DIR/Python"
              echo "Plugin files in $PLUGINS_DIR:"
              ls "$PLUGINS_DIR" | grep -E "OpenSSL|SIP|PyQt5|Python|Qt" || true

              cd /work
              # The kviktor script auto-deduces app_name and package_name
              # from project-dir if not supplied; we pass them explicitly.
              python3.7 pyqt5_android_kviktor.py \
                  --project-dir   "/work/'"$PROJ_DIR"'" \
                  --app-name      "'"$APP_NAME"'" \
                  --package-name  "'"$PACKAGE_NAME"'" \
                  --qt-dir        "/root/Qt/5.13.2" \
                  --ndk-path      "/root/Android/android-ndk-r20b" \
                  --sdk-path      "/root/Android/tools" \
                  --jobs          "$JOBS" \
                  --verbose \
                  2>&1 | tee /work/build.log

              # Find + copy the APK out to /output (which is mounted to host)
              APK=$(find /root/.cache/pyqt5-kviktor-build -name "*.apk" -type f | head -1)
              if [ -z "$APK" ]; then
                  APK=$(find /tmp -name "*.apk" -type f 2>/dev/null | head -1)
              fi
              if [ -z "$APK" ]; then
                  echo "::error::Builder reported success but no APK was found"
                  exit 1
              fi
              cp "$APK" /output/
              echo "✓ APK at /output/$(basename "$APK")"
              ls -la /output/
            '

      - name: Verify APK output
        if: success()
        id: locate
        run: |
          APK=$(find /home/runner/kviktor-output -name "*.apk" -type f | head -1)
          if [ -z "$APK" ]; then
            echo "::error::No APK in /home/runner/kviktor-output/"
            exit 1
          fi
          echo "apk_path=$APK" >> "$GITHUB_OUTPUT"
          ls -lh "$APK"
          file "$APK"
          unzip -l "$APK" | head -30

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: apk-pyqt5-kviktor
          path: ${{ steps.locate.outputs.apk_path }}
          retention-days: 30

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
          if-no-files-found: ignore
          retention-days: 7
